@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<Carrer>  
    <h1>Carreras</h1>
    <p>
        Aquí encontraras las facultades de la universidad, donde veras la facultad donde estudias, o si en el caso que aun no estudias la facultad,
        aqui podras ver lo que aprende un estudiante de esa universidad
        y lo que puedes aprender desde
        antes que empiezas a estudiarlo.
    </p>
<ul style="list-style: none; padding: 0;">
    @foreach (var carrer in Model)
    {
        <li style="background-color: #f0f0f0; border: 1px solid #ddd; border-radius: 5px; margin-bottom: 20px;">
            <img src="@carrer.LogoUrl" alt="@carrer.Name" style="max-width: 100px; height: auto; border-radius: 5px;" />
            <div>
                <a href="@Url.Action("Subject", "Home", new { carrerId = carrer.Id })">
                    <label style="margin: 0 0 5px 0; font-weight: bold;">@carrer.Name</label>
                </a>
                <p style="margin: 0;"> Tipo de Curso: @carrer.AcademicTermType</p>
                <p style="margin: 0;"> Duracion en años: @carrer.DurationInYears</p>
            </div>
            
        </li>
    }
</ul>
@*     <ul>
        @foreach (var carrer in Model)
        {
            <li>
            <a href="@Url.Action("Subject", "Home", new { carrerId = carrer.Id })">
                <img src="@carrer.LogoUrl" alt="@carrer.Name" style="max-width: 100px; height: auto; border-radius: 5px;">
                <label>@carrer.Name</label>
                </a>
            <p>Tipo de curso: @carrer.AcademicTermType</p>
            <p>Duracion en Años: @carrer.DurationInYears</p>            
            </li>
        }
    </ul> *@
